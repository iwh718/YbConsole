<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="iwh.com.simplewen.win0.ybconsole">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
            android:name=".YbApp"
            android:allowBackup="false"
            android:icon="@mipmap/logo"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/logo"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".activity.AddLightApp">
        </activity>
        <activity android:name=".activity.EditApp">
        </activity>
        <activity android:name=".activity.MsgBox">
        </activity>
        <activity android:name=".activity.TestLightApp">
        </activity>
        <activity android:name=".activity.AppInfo">
        </activity>
        <activity android:name=".activity.WikiDetail">
        </activity>
        <activity android:name=".activity.LightMore">
        </activity>
        <activity android:name=".activity.Login">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".activity.MainActivity"
                android:label="@string/app_name">
        </activity>
    </application>

</manifest>